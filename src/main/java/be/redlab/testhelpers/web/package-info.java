/**
 * Web Mocking utils. Empty stub implementations of javax.servlet interfaces. Useful for mocking during testing. The
 * intention is to support the latest servlet specification but the methods are not marked with Java 6 &#064;Override
 * annotation and thus it should also be usable with older versions of the Servlet specification.
 */
package be.redlab.testhelpers.web;